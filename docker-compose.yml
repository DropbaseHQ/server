version: "3.8"
services:
  dropbase-server:
    build:
      context: .
      dockerfile: Dockerfile-server
    volumes:
      - ./workspace:/project/workspace
    ports:
      - "9090:9090"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
  dropbase-lsp:
    build:
      context: .
      dockerfile: Dockerfile-lsp
    volumes:
      - ./workspace:/project/workspace
    ports:
      - "9091:9091"
    environment:
      - PYTHONUNBUFFERED=1